export default function Settings() {
  return <><p className="font-mono text-[11px] tracking-[0.2em] text-emerald-300">SYSTEM / SETTINGS</p><h1 className="mt-3 font-display text-4xl font-light tracking-wide text-white">CMS SETUP</h1><div className="mt-10 max-w-2xl divide-y divide-white/10 border-y border-white/10 text-sm leading-6 text-white/55"><section className="py-6"><h2 className="font-display text-xl font-light tracking-wide text-white">01 / Configure environment</h2><p className="mt-2">Copy `cms/.env.example` to `cms/.env.local` and supply the Supabase URL and publishable key. The service role key is reserved for trusted server-only jobs.</p></section><section className="py-6"><h2 className="font-display text-xl font-light tracking-wide text-white">02 / Apply database migration</h2><p className="mt-2">Run `supabase/migrations/0001_cms.sql` in the connected project. Add approved user IDs to `cms_admins`; a normal Supabase account alone cannot access the CMS.</p></section><section className="py-6"><h2 className="font-display text-xl font-light tracking-wide text-white">03 / Configure the website</h2><p className="mt-2">Set equivalent public Supabase variables in `website/.env.local`, then seed the existing hardcoded data into the new tables before switching public reads.</p></section></div></>;
}
